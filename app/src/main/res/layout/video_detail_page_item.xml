<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="115dp"
    android:layout_height="45dp"
    android:layout_margin="@dimen/item_half_spacing"
    android:background="@drawable/selector_video_page_background"
    android:foreground="?selectableItemBackground">

    <TextView
        android:id="@+id/tv_page_title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.App.Medium"
        android:textColor="@color/selector_text_black_selected_primary" />

</FrameLayout>